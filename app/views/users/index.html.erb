<div class='container px-5 px-sm-0'>
  <div class='row'>
    <div class='col-md-8 offset-md-1'>
            
      <h3>Users</h3>
       <%= search_form_for @q do |f| %>
        <%#= f.label :name, "Keyword" %>
        <%= f.search_field :name_cont, placeholder:"Search" %>
      
        <%= f.submit "検索" %>
      <% end %>
      <%#= render 'index',users: @users %>
      
      <div class="d-lg-flex d-md-flex my-box-light flex-wrap mt-3">
    <% @users.each do |user| %>
    <div class="my-box-member col-sm-3">
      <%= link_to user_path(user) do %>
            <%= attachment_image_tag user, :profile_image, :fill, 120, 120, fallback: "no_image.jpg", size:'120x120' ,class: 'rounded-circle' %>
      <% end %>
      <p><%= user.name %></p>
    </div>
    <% end %>
  </div>
      
    </div>
  </div>
</div>
